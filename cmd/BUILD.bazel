load("@rules_go//go:def.bzl", "go_library")

go_library(
    name = "cmd",
    srcs = ["doc.go"],
    importpath = "github.com/regalium-os/osdp-go/cmd",
    visibility = ["//visibility:public"],
)

# The architecture test reads these as data; without them it would run blind.
# keep
filegroup(
    name = "sources",
    srcs = glob(["**/*.go"]),
    visibility = ["//visibility:public"],
)
